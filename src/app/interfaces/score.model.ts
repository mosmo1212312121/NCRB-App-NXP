import { Initial } from './initial.model';

export class Score {
  scoreA: number;
  scoreB: number;
  scoreC: number;
  scoreTotal: number;
  requirement?: string;
  mdrLevel?: string;
  risk?: string;
  info?: Initial;
}
